# Copilot master prompt

Brug denne prompt først i GitHub Copilot Chat:

> I am building an online multiplayer Yatzy game for Chrome browser. Use the markdown files in the docs folder as the source of truth. The stack must be Angular + TypeScript on the frontend with a proper service layer for API and SignalR calls, ASP.NET core 9 Web API for controllers, C# class libraries for domain and logic, and Entity Framework Core for database persistence. The solution must be split into clean layers and built step by step. Keep the backend authoritative for dice rolls, scoring, and turn validation. Follow the uploaded mockup and preserve the classic green Yatzy score sheet feel. Do not jump ahead too much. Complete one phase at a time and keep the code production-oriented and structured for maintainability.
